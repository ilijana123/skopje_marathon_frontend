import { styled } from '@mui/material/styles';
import { Card, Grid, Button } from '@mui/material';

export const RootGrid = styled(Grid)({
    justifyContent: 'center',
});

export const Form Card = styled(Card)(({ theme }) => ({